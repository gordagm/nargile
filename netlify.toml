# Next.js + Netlify
# Next.js runtime'ı açıkça etkinleştir (eski statik ayarı geçersiz kılar).
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
